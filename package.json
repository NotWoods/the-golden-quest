{
  "name": "alexa-dungeon-crawler",
  "version": "1.0.0",
  "main": "out/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "tsc",
    "prestart": "npm run prepare --silent || exit 0",
    "start": "node out/index.js"
  },
  "keywords": [
    "alexa",
    "skill",
    "game"
  ],
  "dependencies": {
    "alexa-sdk": "^1.0.6"
  },
  "devDependencies": {
    "typescript": "^2.2.1",
    "@types/alexa-sdk": "^1.0.4"
  }
}
