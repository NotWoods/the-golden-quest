{
  "name": "alexa-dungeon-crawler",
  "version": "1.0.0",
  "main": "out/index.js",
  "scripts": {
    "prepare": "tsc || exit 0",
    "prestart": "npm run prepare --silent",
    "start": "node out/index.js",
    "pretest": "npm run prepare --silent > nul",
    "test": "tape out/test/**/*.spec.js"
  },
  "keywords": [
    "alexa",
    "skill",
    "game"
  ],
  "dependencies": {
    "alexa-sdk": "^1.0.6",
    "denodeify": "^1.2.1",
    "questions": "0.0.6",
    "shortid": "^2.2.8",
    "string.prototype.startswith": "^0.2.0"
  },
  "devDependencies": {
    "@types/alexa-sdk": "^1.0.4",
    "@types/blue-tape": "^0.1.30",
    "@types/denodeify": "^1.2.30",
    "@types/node": "^7.0.8",
    "@types/shortid": "0.0.28",
    "blue-tape": "^1.0.0",
    "typescript": "^2.2.1"
  }
}
